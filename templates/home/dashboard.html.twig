{% extends 'base.html.twig' %}

{% block title %}Панель{% endblock %}

{% block body %}
    <section class="section section--white">
        <div class="container">
            <h1>Панель</h1>
            <p>Вы вошли как <strong>{{ app.user.email }}</strong> ({{ app.user.role.value }}).</p>
            <p>
                {% include 'components/button.html.twig' with { variant: 'secondary', text: 'На главную', href: path('app_home') } %}
            </p>
        </div>
    </section>
{% endblock %}