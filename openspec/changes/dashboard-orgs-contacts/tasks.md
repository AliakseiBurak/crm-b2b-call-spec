## 1. Данные списка организаций

- [x] 1.1 Добавить `OrganizationRepository::findForDashboard(?User $user, ?string $search, string $sort, string $dir): array` — один DQL-запрос: фильтр области доступа через `findAccessibleIds($user)` (`o.id IN (:ids)`, при `null` — без фильтра), коррелированные подзапросы `lastMadeAt = MAX(c.made_at)` и `nextScheduledAt = MIN(c.scheduled_at)` с `scheduled_at >= NOW()`
- [x] 1.2 Добавить в запрос поиск: `o.name LIKE :term` + `EXISTS` по контактам (`c2.name LIKE :term OR c2.phone LIKE :term OR c2.email LIKE :term`), параметр `%term%`, регистронезависимо (как позволяет БД)
- [x] 1.3 Добавить whitelist-маппинг сортировки (`name`, `industry`, `lastCall`, `nextCall`) → колонка/выражение в ORDER BY; по умолчанию `o.name ASC`; вторичный ключ `o.name ASC` для стабильности; при сортировке по датам `NULL` (нет следующего звонка) — в конец
- [x] 1.4 Создать `ContactRepository::findByOrganizations(array $ids): array` — все контакты выбранных организаций одним запросом (organization IN ids, join организации)
- [x] 1.5 Добавить в `findForDashboard` коррелированный подзапрос `lastCallNote` — непустая `notes` звонка с максимальной эффективной датой `COALESCE(made_at, scheduled_at)` среди звонков с заметками (2-уровневый подзапрос в SELECT); при равных эффективных датах побеждает звонок с наибольшим `Call.id`
- [x] 1.6 Создать `CallRepository::findAllCallsByOrganizations(array $ids): array` — id организации, `COALESCE(made_at, scheduled_at)` как дата, `notes` (nullable) и id контакта, все звонки организации (не только с заметками), ORDER BY дата DESC, одним запросом

## 2. Рендер секции на панели

- [x] 2.1 Создать DTO `DashboardOrganizationRow` (organization, lastMadeAt, nextScheduledAt, lastCallNote, lastCallDate, lastCallContactId) и заполнить его в `HomeController::dashboard()`; один вызов `findAccessibleIds($user)` передаётся в статистику, таблицу, контакты и заметки
- [x] 2.2 Создать partial `templates/home/partials/_organizations_table.html.twig`: форма поиска `?q=&sort=&dir=` GET с крестиком очистки `type="search"` (авто-применение по `input`, `Enter` → `Найти`), зебра-таблица без рамок, колонки «Название» (сортировочный заголовок), «Сфера деятельности» (сортировочный), «Последний звонок» (сортировочный), «Следующий звонок» (сортировочный), заглушка дат «—» при отсутствии звонков, сообщение «Ничего не найдено» при пустом результате
- [x] 2.3 Под каждой строкой — нативный `<details>/<summary>`: раскрытие «Звонки и контакты организации»; внутри карточки контактов по существующей дизайн-системе (имя, телефон `tel:`, email `mailto:`, вторичные данные, кнопка «Изменить» в границах карточки — слева внизу, через `card_footer`), кнопка «Добавить контакт» `btn--sm`; при пустом списке отображается только кнопка «Добавить контакт»; контакты передаются сгруппированными `contactsByOrganization[orgId]`
- [x] 2.3a В раскрытой секции над контактами — блок звонков: заметка последнего звонка (`lastCallNote` + `lastCallDate` из DTO) и контакт этого звонка (`contactById[lastCallContactId]` — имя, `tel:`, `mailto:`) рядом с заметкой; вложенный `<details>` «Все звонки» со списком строк «дата + текст» из `callsByOrganization[orgId]`; строки звонков без заметок показывают дату и контакт (если есть) — без текста; секция не рендерится, если звонков нет
- [x] 2.3b Кнопка «Добавить звонок» — перед блоком «Последний звонок» в раскрытой секции, ссылка на `organizations/{id}/calls/new` (404); кнопка видна всегда, в том числе когда звонков нет
- [x] 2.4 Вставить partial в `templates/home/dashboard.html.twig` ниже блока «Ждут обзвона»; даты форматируются `d.m.Y`; индикаторы направления сортировки в заголовках
- [x] 2.5 Добавить SCSS: стилизация `<details>` (стрелка/поворот, отступы), поискового поля и активных сортировочных заголовков — в рамках существующей дизайн-системы (`web-interface`), скомпилировать ассеты
- [x] 2.6 Кнопка «Изменить» в строке организации — ссылка на `organizations/{id}/edit` (маршрут не реализован, 404), стиль кнопки дизайн-системы
- [x] 2.7 Кнопка «Изменить» в каждой строке списка «Все звонки» — ссылка на `calls/{id}/edit` (404), прижата к правому краю строки (`.org-calls__item` flex, `.org-calls__edit`); кнопка «Изменить дату» у следующего звонка удалена; дата следующего звонка выровнена по правому краю ячейки (`.org-table__next-call`)
- [x] 2.8 Кнопка «Добавить контакт» в раскрытой секции контактов — ссылка на `organizations/{id}/contacts/new` (404)
- [x] 2.9 Кнопка «Изменить» на каждой карточке контакта — ссылка на `contacts/{id}/edit` (404)
- [x] 2.10 Карточка контакта рендерит непустую `Contact.notes` под меткой «Заметка: …» (card.html.twig); карточка не содержит кнопок звонка
- [x] 2.11 Сортировочные заголовки — обычные кликабельные ссылки с маркером направления (↑/↓) и пометкой активной колонки; pill-стилизация «карточек»-кнопок не применяется (отменено по решению); по умолчанию таблица отсортирована по названию А–Я (fix: при пустом `sort` в запрос добавляется `ORDER BY o.name ASC`)
- [x] 2.12 SCSS: карточки контактов `flex: 0 1 280px` (не растягиваются на всю ширину), hover-подсветка карточки зелёной дымкой `#e7f3dd` — отличной от зебры таблицы
- [x] 2.14 Hover строк таблицы: оттенок того же голубого семейства `#cfe6f2` (токен `$color-table-stripe-hover`) вместо `transparent` — зебра не «убирается», а подсвечивается; аккордеонная строка `.org-details` при наведении не подсвечивается; дизайн-система `web-interface` «Таблицы» обновлена (наведение подсвечивает строку)
- [x] 2.13 Фикстуры: контакты по организациям (Ромашка 2, Вектор 4 включая «Приёмную», Сидоров 1, Конкурент 2, Горизонт 0 — без контактов, Закат 1 — с контактом и без звонков) — у Вектора больше 3, чтобы карточки переносились на новую строку (влияет на вид) — с именами-людьми («Иван Петрович Иванов», «Иван Иванович Петров» и т.п.), фамилии не у всех; имена не обязаны быть уникальными ни в организации (у Вектора и Конкурента общая «Марина Александровна», глобально), ни глобально; должности (ген. директор / отдел закупок / отдел кадров и др.) — разные для всех контактов, дубликатов нет, `Contact.notes` у локальных индексов 0,2; звонки: Ромашка — три звонка с разными наборами (факт −10д без заметки и без контакта, факт −3д только с заметкой «Нет ответа…», план +1д только с контактом); Вектор — факт и план с заметками; Сидоров — план +45д с заметкой, но **без контакта** (последний звонок) и просроченный план −2д с контактом без заметки; Конкурент — звонки с заметками и контактами (вне области менеджера) плюс «только с датой» (факт −9д, план +14д); Горизонт — без звонков вовсе; Закат — с контактом, без звонков; Горизонт и Закат в области доступа менеджера (personal1)

## 3. Тесты (Playwright E2E — нет PHPUnit в проекте)

- [x] 3.1 E2E: менеджер видит только организации области доступа (Ромашка, Вектор, Сидоров, Горизонт, Закат; НЕ Конкурент — ADR-0007); администратор — все 6; даты звонков из реальных `Call` (lastMadeAt из `made_at`, nextScheduledAt из будущего `scheduled_at`); у организаций без звонков (Горизонт, Закат) обе даты — «—», «Ничего не найдено» при пустом поиске; organisation без заметок (Горизонт) не рендерит секцию звонков
- [x] 3.2 E2E: поиск по названию организации и по имени/телефону/email контакта (фильтрация организаций); без совпадений — «Ничего не найдено»; очистка через нативный крестик `type="search"` возвращает полный список
- [x] 3.3 E2E: сортировка по названию и отрасли (А–Я/Я–А, первый клик = ASC при `default sort=''`), по последнему и следующему звонку (ближайший сверху, без дат — в конец); вторичный ключ по названию А–Я
- [x] 3.4 E2E-тест раскрытия контактов (аккордеон `<details>`): клик по строке показывает карточки контактов (имя «Иван Петрович», `tel:`-ссылка, `mailto:`-ссылка), кнопка «Изменить» в границах карточки слева внизу (`.org-contacts__edit`); повторный клик скрывает; организация без контактов и звонков (Горизонт) показывает только кнопки «Добавить контакт» и «Добавить звонок» и не рендерит секцию звонков; организация с контактом, но без звонков (Закат) показывает карточку контакта и не рендерит секцию звонков; карточка контакта с заполненной заметкой показывает строку «Заметка: …»; summary аккордеона — «Звонки и контакты организации»
- [x] 3.4a E2E-тесты звонков: в раскрытой секции показана заметка последнего по времени звонка (макс. `COALESCE(made_at, scheduled_at)` среди непустых заметок) — у Ромашки единственная заметка у звонка без контакта (-3д), поэтому рядом с заметкой контакт не отображается; «Все звонки (3)» Ромашки раскрывает все звонки с датами, новые сверху: план +1д — дата + контакт «Иван Петрович» (`tel:`-ссылка, `mailto:`-ссылка) без текста; факт -3д — дата + текст «Нет ответа, перезвонить завтра» без контакта; факт -10д — только дата; у каждой строки справа кнопка «Изменить» → `calls/{id}/edit` (404); у организаций без звонков (Горизонт, Закат) секция отсутствует, но кнопка «Добавить звонок» → `organizations/{id}/calls/new` (404) видна; блок «Последний звонок» не рендерится, если непустых заметок нет
- [!] 3.5 E2E-тесты 404-заглушек — **отменено** (тест удалён; маршруты `organizations/{id}/edit`, `calls/{id}/edit`, `organizations/{id}/calls/new`, `contacts/{id}/edit` будут реализованы позже)
- [x] 3.6 Регрессия: `make e2e` — старые e2e (dashboard.spec.ts, design-components.spec.ts, smoke.spec.ts, design-mobile.spec.ts, design-visual.spec.ts) проходят без регрессий (25/25 passed)
