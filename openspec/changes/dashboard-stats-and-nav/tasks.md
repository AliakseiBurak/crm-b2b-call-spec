## 1. Статистика дашборда (capability dashboard)

- [ ] 1.1 Добавить в `OrganizationRepository` метод `findAccessibleIds(User $user)`: для админа возвращает `null` (все организации), для менеджера — id организаций из `user-<id>-group` и assigned custom групп (ADR-0007, без новых ACL-механизмов)
- [ ] 1.2 Добавить в `CallRepository` метод `dashboardStats(?array $organizationIds): DashboardStats` — один DQL `COUNT(CASE ...)` по `call`: обзвонено сегодня (made_at ≥ начало дня), за 7 дней (≥ начало дня − 6), за 30 дней (≥ начало дня − 29); ждут обзвона сегодня (scheduled_at в [начало дня, конец дня]), в ближайшие 7 дней (`scheduled_at` в [сейчас, сейчас+7д]), в ближайшие 30 дней ([сейчас, сейчас+30д]); фильтр `IN (:orgIds)` для менеджера, без фильтра для админа
- [ ] 1.3 Создать readonly-DTO `DashboardStats` (6 полей: calledToday, calledWeek, calledMonth, waitingToday, waitingWeek, waitingMonth)
- [ ] 1.4 В `HomeController::dashboard()` получить скоуп (`findAccessibleIds`), вызвать `dashboardStats()`, передать в шаблон; страница отдаёт 200 без падения и без записей в лог ошибок
- [ ] 1.5 Переписать `home/dashboard.html.twig`: h1 «Панель», строка «Вы вошли как …», блок «Обзвонено» (3 виджета на зелёной градиентной ленте, числа 56px bold) и белая секция «Ждут обзвона» (3 виджета, числа оранжевым акцентом); фолбэк «0» при отсутствии данных; никаких захардкоженных значений
- [ ] 1.6 Проверить вручную через curl/логин: у админа и у менеджера дашборд рендерится, числа совпадают со звонками в области доступа

## 2. Шапка и навигация

- [ ] 2.1 Переписать `components/header.html.twig`: верхняя строка = логотип «B2B Call CRM» (ссылка на главную) слева, навигационные ссылки в строке (без градиентной полосы), для `app.user` справа кнопки «Создать организацию» (`/organizations/new`) и «Создать контакт» (`/contacts/new`); параметр `menu` без пункта «Главная»
- [ ] 2.2 Обновить `base.html.twig`: меню вошедших = «Панель», «Выйти»; гостей = «Войти»; удалить `{% embed modal.html.twig %}` и форму «Заказать звонок»
- [ ] 2.3 Удалить модалку целиком: `components/modal.html.twig`, `assets/js/modal.js`, `assets/js/call-request.js`, стили модалки; убрать их импорты из `assets/app.js`
- [ ] 2.4 Обновить SCSS шапки: убрать зелёную градиентную полосу `header__menu` и CTA-пару (`cta-bar--call`, `cta-bar--create`), оставить белую верхнюю строку с логотипом/навигацией; проверить `npm run build` без ошибок
- [ ] 2.5 Проверить: гость видит «Войти» без кнопок создания; вошедший — «Панель», «Выйти» и обе кнопки создания; клик логотипа ведёт на главную; `/organizations/new` и `/contacts/new` отдают 404 (заглушки)

## 3. Welcome page (упрощение)

- [ ] 3.1 В `home/index.html.twig` удалить промо-секцию «Как это работает» и секцию «Программы обучения команды продаж» (offer-карточки, `btn--all`), убрать вторичную кнопку «Заказать звонок» из hero; оставить кнопку «Начать работу» (→ дашборд) и блок статистики
- [ ] 3.2 Удалить ставшие мёртвыми SCSS-ветки (`.promo*`, `.offer-card*`, `.btn--all`, `.stats`-дубли при необходимости), пересобрать ассеты, проверить hero и статистический блок на живом сайте

## 4. Фикстуры и данные

- [ ] 4.1 Дополнить `AppFixtures` звонками: made_at сегодня/вчера/6–7/29–30 дней назад, scheduled_at сегодня/через 3/20 дней — по организациям всех периодов; у части звонков организация вне области доступа менеджера (для проверки ADR-0007)
- [ ] 4.2 Перезагрузить фикстуры (`php bin/console doctrine:fixtures:load` на сайте), проверить: дашборд показывает ненулевые числа, у менеджера числа меньше/равны админским, «внешние» звонки не влияют

## 5. E2E и верификация

- [ ] 5.1 Обновить `e2e/tests/smoke.spec.ts`: после входа в меню «Панель»/«Выйти» (без «Главная»), кнопки создания видны; у гостя их нет; логотип — ссылка на главную
- [ ] 5.2 Обновить устаревшие проверки: убрать тесты/ассерты на `cta-bar--call`, модалку `modal-call`, offer-карточки, `btn--all`, промо (design-tokens/components/semantics/mobile/visual) и заменить на новые: отсутствие модалки/CTA, меню в верхней строке
- [ ] 5.3 Написать `e2e/tests/dashboard.spec.ts`: 6 виджетов с ожидаемыми числами после фикстур (у админа и менеджера, скоуп ADR-007), числа 56px bold, зелёная лента «Обзвонено», белая секция «Ждут обзвона»
- [ ] 5.4 Полный прогон `npm test` (все спеки) — 100% зелёные; финальная ручная проверка всех страниц на живом сайте