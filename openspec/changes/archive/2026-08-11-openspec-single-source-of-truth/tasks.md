# Tasks: openspec-single-source-of-truth

## 1. ADR-0006 (единый источник истины)

- [x] 1.1 Подтвердить, что `adr/0006-openspec-single-source-of-truth.md` существует: OpenSpec — единственный источник истины, PRD удаляется, реестр отложенных пробелов (пользователи §2.10, NFR §7, CSRF §6.3).
- [x] 1.2 Подтвердить, что манифест `adr.md` заполнен и перечисляет ADR-0006.

## 2. Переписывание контекста ADR-0001–0004

- [x] 2.1 `adr/0001-manager-own-group.md`: секция Context ссылается на `access-control` и `organization-groups` вместо «PRD §2.10, §2.11, §3.7»; Decision/Consequences не меняются.
- [x] 2.2 `adr/0002-org-group-many-to-many.md`: секция Context ссылается на `organization-groups` вместо «PRD §2.12, §3.7».
- [x] 2.3 `adr/0003-manager-operator-access-scope.md`: секция Context ссылается на `access-control` вместо «PRD §3.7».
- [x] 2.4 `adr/0004-admin-global-scope.md`: секция Context ссылается на `access-control` вместо «PRD §1.1, §3.7».

## 3. Проверка

- [x] 3.1 Проверить, что в `adr/` не осталось ссылок «PRD §…» (grep), кроме ADR-0006 (там — исторический реестр).

## 4. Roadmap: план разработки (порт PRD §8) — будущие этапы, реализуются отдельными изменениями

- [ ] 4.1 Этап 1: MVP — модели данных и миграции; CRUD организаций, контактов, курсов; поиск организаций; базовая админка.
- [ ] 4.2 Этап 2: Рассылки — email-кампании; отписки; статистика.
- [ ] 4.3 Этап 3: Звонки и анализ — модуль звонков; запись и анализ; генерация писем.
- [ ] 4.4 Этап 4: Интеграции и настройки — телефония; внешние источники; очереди и фоновые задачи; система ролей и прав доступа.
