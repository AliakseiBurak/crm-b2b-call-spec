## 1. Статистика дашборда (capability dashboard)

- [x] 1.1 Добавить в `OrganizationRepository` метод `findAccessibleIds(User $user)`: для админа возвращает `null` (все организации), для менеджера — id организаций из `user-<id>-group` и assigned custom групп (ADR-0007, без новых ACL-механизмов)
- [x] 1.2 Добавить в `CallRepository` метод `dashboardStats(?array $organizationIds): DashboardStats` — один DQL `COUNT(CASE ...)` по `call`: обзвонено сегодня (made_at ≥ начало дня), за 7 дней (≥ начало дня − 6), за 30 дней (≥ начало дня − 29); ждут обзвона сегодня (scheduled_at в [начало дня, конец дня]), в ближайшие 7 дней (`scheduled_at` в [сейчас, сейчас+7д]), в ближайшие 30 дней ([сейчас, сейчас+30д]); фильтр `IN (:orgIds)` для менеджера, без фильтра для админа
- [x] 1.3 Создать readonly-DTO `DashboardStats` (6 полей: calledToday, calledWeek, calledMonth, waitingToday, waitingWeek, waitingMonth)
- [x] 1.4 В `HomeController::dashboard()` получить скоуп (`findAccessibleIds`), вызвать `dashboardStats()`, передать в шаблон; страница отдаёт 200 без падения и без записей в лог ошибок
- [x] 1.5 Переписать `home/dashboard.html.twig`: h1 «Панель», строка «Вы вошли как …», блок «Обзвонено» (3 виджета на зелёной градиентной ленте, числа 56px bold) и белая секция «Ждут обзвона» (3 виджета, числа оранжевым акцентом); фолбэк «0» при отсутствии данных; никаких захардкоженных значений
- [x] 1.6 Проверить вручную через curl/логин: у админа и у менеджера дашборд рендерится, числа совпадают со звонками в области доступа

## 2. Шапка и навигация

- [x] 2.1 Переписать `components/header.html.twig`: верхняя строка = логотип «B2B Call CRM» (ссылка на главную) слева, навигационные ссылки в строке (без градиентной полосы), для `app.user` справа кнопки «Создать организацию» (`/organizations/new`) и «Создать контакт» (`/contacts/new`); параметр `menu` без пункта «Главная»
- [x] 2.2 Обновить `base.html.twig`: меню вошедших = «Панель», «Выйти»; гостей = «Войти»; удалить `{% embed modal.html.twig %}` и форму «Заказать звонок»
- [x] 2.3 Удалить модалку целиком: `components/modal.html.twig`, `assets/js/modal.js`, `assets/js/call-request.js`, стили модалки; убрать их импорты из `assets/app.js`
- [x] 2.4 Обновить SCSS шапки: убрать зелёную градиентную полосу `header__menu` и CTA-пару (`cta-bar--call`, `cta-bar--create`), оставить белую верхнюю строку с логотипом/навигацией; проверить `npm run build` без ошибок
- [x] 2.5 Проверить: гость видит «Войти» без кнопок создания; вошедший — «Панель», «Выйти» и обе кнопки создания; клик логотипа ведёт на главную; `/organizations/new` и `/contacts/new` отдают 404 (заглушки)

## 3. Welcome page (упрощение)

- [x] 3.1 В `home/index.html.twig` удалить промо-секцию «Как это работает» и секцию «Программы обучения команды продаж» (offer-карточки, `btn--all`), убрать вторичную кнопку «Заказать звонок» из hero и удалить блок статистики (1 287/342/26%); оставить кнопку «Начать работу» (→ дашборд)
- [x] 3.2 Удалить ставшие мёртвыми SCSS-ветки (`.promo*`, `.offer-card*`, `.btn--all`), пересобрать ассеты, проверить hero на живом сайте

## 4. Фикстуры и данные

- [x] 4.1 Дополнить `AppFixtures` звонками: made_at сегодня/вчера/6–7/29–30 дней назад, scheduled_at сегодня/через 3/20 дней — по организациям всех периодов; у части звонков организация вне области доступа менеджера (для проверки ADR-0007)
- [x] 4.2 Перезагрузить фикстуры (`php bin/console doctrine:fixtures:load` на сайте), проверить: дашборд показывает ненулевые числа, у менеджера числа меньше/равны админским, «внешние» звонки не влияют

## 5. E2E и верификация

- [x] 5.1 Обновить `e2e/tests/smoke.spec.ts`: после входа в меню «Панель»/«Выйти» (без «Главная»), кнопки создания видны; у гостя их нет; логотип — ссылка на главную
- [x] 5.2 Удалить e2e-проверки стилей (design-tokens, design-semantics, design-responsive, тач-цели, скриншоты): достаточно проверять наличие элементов; стилевые тесты возвращаются на финальном этапе
- [x] 5.3 Написать `e2e/tests/dashboard.spec.ts`: 6 виджетов и подписи присутствуют, h1 «Панель», «Вы вошли как …» (админ/менеджер, скоуп ADR-007); числовые значения не ассертируются (фикстуры загружаются один раз)
- [x] 5.4 Полный прогон `npm test` (все спеки) — 100% зелёные; финальная ручная проверка всех страниц на живом сайте