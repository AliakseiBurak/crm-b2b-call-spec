## 1. Миграция и индекс

- [x] 1.1 Добавить в миграцию `Version20260817000000` составной индекс `IDX_call_org_made (organization_id, made_at)` на таблицу `call`; добавить DROP в `down()`

## 2. Запрос данных индикаторов

- [x] 2.1 Вынести маппинг периодов (CASE-категории) в общую константу/метод `CallRepository`, используемую и `dashboardStats()`, и новым `organizationCounts()` — единый источник истины периодов; включить границы `yesterdayStart/yesterdayEnd` для просроченных
- [x] 2.2 Добавить `CallRepository::organizationCounts(array $organizationIds, \DateTimeImmutable $now): array` — нативный SQL (DBAL): `COUNT(DISTINCT c.organization_id)` с 9 CASE-категориями: 3 called (made_at), 3 waiting (scheduled_at в будущем + NOT EXISTS), 3 overdue (scheduled_at в прошлом + made_at IS NULL); возвращает `[bucket => n]`; категории без организаций возвращают `0`

## 3. Контроллер и передача данных

- [x] 3.1 В `HomeController::index()` добавить загрузку статистики: вызвать `dashboardStats()`, `organizationCounts()`, вычислить Y = `count($organizationIds)` (для admin: `count($organizationRepository->count([]))`), собрать `statsByOrg: [bucket => n]` для 9 бакетов и передать `totalOrgs: Y` отдельной переменной; для гостей (нет `app.user`) — рендерить только hero без статистики
- [x] 3.2 В `HomeController::dashboard()` оставить только таблицу организаций; удалить рендер секции статистики из шаблона `dashboard.html.twig`
- [x] 3.3 Принять параметр `?filter=` из URL, передать в шаблон (для будущего пре-фильтра)

## 4. Шаблон и SCSS

- [x] 4.1 В `home/index.html.twig`: после hero — карточка `.stats__total` «Доступно организаций: Y» на всю ширину; под ней — три секции статистики (Звонков / Ожидают / Просроченные), в каждой — по 3 показателя с индикаторами; для `app.user`; гостям — только hero
- [x] 4.2 В `dashboard.html.twig`: удалить секцию статистики (оставить только таблицу организаций)
- [x] 4.3 Переименовать подписи показателей: «Обзвонено» → «Звонков»; «Сегодня/В течение недели/В течение месяца» → «Ожидают сегодня/на неделе/в месяце»; добавить «Просроченные: вчера/за 7 дней/за 30 дней»
- [x] 4.4 Под каждым из девяти показателей — ссылка `.stats__orgs` «По организациям: N» с href `/dashboard?filter=<bucket>`; при N=0 — ссылка отображается
- [x] 4.5 SCSS: стили `.stats__total` (карточка на всю ширину, жирный текст, отступы), `.stats__orgs` (мелкий серый текст, ссылка без подчёркивания, hover-подчёркивание); три секции статистики с заголовками «Звонков», «Ожидают», «Просроченные»

## 5. Тесты (Playwright E2E)

- [x] 5.1 E2E: вошедший пользователь видит карточку «Доступно организаций: Y» на `/` и 9 показателей под ней
- [x] 5.2 E2E: гость на `/` видит только hero, без карточки и статистики
- [x] 5.3 E2E: после логина редирект на `/`, карточка и статистика видны
- [x] 5.4 E2E: на `/dashboard` карточка и статистика НЕ отображаются, только таблица организаций
- [x] 5.5 E2E: под каждым показателем отображается «По организациям: N»; ссылка содержит `?filter=<bucket>`
- [x] 5.6 E2E: Y в карточке = число организаций области доступа (менеджер vs администратор)
- [x] 5.7 E2E: клик по ссылке переходит на `/dashboard?filter=<bucket>`
- [x] 5.8 E2E: исключающая логика — организация с `made_at` сегодня НЕ учитывается в «Ожидают сегодня»
- [x] 5.9 E2E: просроченные — организация с `scheduled_at` вчера и `made_at IS NULL` учитывается в «Просроченные: вчера»
- [x] 5.10 E2E: просроченные + звонки — организация с просроченным вчера И совершённым сегодня учитывается в обеих категориях
- [x] 5.11 E2E: просроченные частичные — организация с 5 запланированными на вчера (3 совершены, 2 нет) учитывается в «Просроченные: вчера»
- [x] 5.12 E2E: пустая категория — «По организациям: 0» с присутствующей ссылкой
- [x] 5.13 E2E: подписи показателей — новые подписи на месте, старые отсутствуют
- [x] 5.14 Регрессия: `make e2e` (все существующие спецификации) — без регрессий

## 6. Подготовительные данные (выполнено в dashboard-orgs-contacts)

- [x] 6.1 Фикстуры покрывают все категории индикаторов (факты: сегодня/вчера/10/28 дней; планы: сегодня/1–3/20/45 дней)
- [x] 6.2 Добавить фикстуры для просроченных: организации с `scheduled_at` вчера/5 дней назад/20 дней назад без `made_at`; организации с комбинацией просроченных и совершённых звонков
