# Calls

Модуль телемаркетинговых звонков: единая сущность `Call` для планирования,
проведения обзвона и фиксации результата (рассылка / сделка / будущий звонок).
Модель определена в ADR-0004.

## Purpose

Звонки: планирование и напоминание (будущие звонки), пошаговый сеанс обзвона
по группе организаций, фиксация результата (одна рассылка, сделка, следующий
звонок) и факта звонка (кто и когда).

## Requirements

### Requirement: Сущность звонка
The system SHALL store a call with an organization, an optional contact, a
scheduled date, and SHALL record the fact of the call (who made it and when)
after the call takes place.

#### Scenario: Запланированный звонок
- **WHEN** в системе существует организация "ООО Ромашка" и контакт "Иван Петров"
- **AND** менеджер планирует звонок контакту "Иван Петров" на завтра
- **THEN** звонок сохраняется с датой "завтра"

#### Scenario: Фиксация факта звонка
- **WHEN** менеджер "Иван Петров" провёл звонок контакту организации "ООО Ромашка"
- **AND** отмечает дату и время звонка
- **THEN** в истории звонков организации отображается, что звонок осуществил менеджер "Иван Петров" в указанные дату и время

### Requirement: Будущие звонки и напоминание
A call SHALL be considered a future call when its scheduled date is in the
future; such calls SHALL appear in planning and the reminder.

#### Scenario: Звонок на сегодня
- **WHEN** у организации "ООО Ромашка" есть звонок, запланированный на сегодня
- **AND** менеджер открывает псевдогруппу «Звонки на сегодня»
- **THEN** организация "ООО Ромашка" отображается в этой псевдогруппе

#### Scenario: Организации без будущих звонков
- **WHEN** у организации "ООО Ромашка" нет звонка с датой в будущем
- **AND** менеджер открывает псевдогруппу «Будущие звонки отключены»
- **THEN** организация "ООО Ромашка" отображается в этой псевдогруппе

### Requirement: Сортировка списка обзвона
The system SHALL sort the call list by the next-call date (overdue first), by
the last call date, or by the last note date, at the manager's choice.

#### Scenario: Сортировка по следующему звонку
- **WHEN** организации "ООО А" и "ООО Б" имеют следующие звонки с датами, причём "ООО Б" просрочена
- **AND** менеджер сортирует список обзвона по дате следующего звонка
- **THEN** первой в списке отображается организация "ООО Б"

### Requirement: Сеанс обзвона по группе организаций
The system SHALL let a manager run a call session over an organization group:
select an organization, show its info and contacts, record notes and result,
then move to the next organization.

#### Scenario: Сеанс обзвона
- **WHEN** менеджер запускает обзвон по своей группе организаций
- **AND** выбирает организацию "ООО Ромашка"
- **THEN** на экране отображается карточка организации "ООО Ромашка" и её контакты
- **AND** менеджер может оставить заметки и отметить результат звонка

#### Scenario: Переход к следующей организации
- **WHEN** менеджер завершил звонок по организации "ООО Ромашка"
- **AND** сохраняет заметки и результат
- **THEN** система переходит к следующей организации в списке обзвона

### Requirement: Результат звонка
The call result SHALL be a combination of independent marks: at most one
campaign (see `campaigns`), a deal, and a next call; a call MAY have none of
them, recording only the fact of the call.

#### Scenario: Результат — рассылка
- **WHEN** менеджер завершает звонок по организации "ООО Ромашка"
- **AND** выбирает из списка рассылку "Новые курсы"
- **THEN** организация "ООО Ромашка" помечается получателем рассылки "Новые курсы" по итогам звонка

#### Scenario: Результат — сделка
- **WHEN** менеджер завершает звонок по организации "ООО Ромашка"
- **AND** отмечает "сделка совершена"
- **THEN** в карточке звонка отображается отметка о совершённой сделке

#### Scenario: Результат — будущий звонок
- **WHEN** менеджер завершает звонок по организации "ООО Ромашка"
- **AND** указывает дату следующего звонка
- **THEN** создаётся новый звонок с этой датой, связанный со звонком
- **AND** организация "ООО Ромашка" попадает в планирование/напоминание

#### Scenario: Комбинация результатов
- **WHEN** менеджер завершает звонок по организации "ООО Ромашка"
- **AND** одновременно выбирает рассылку "Новые курсы", отмечает сделку и назначает следующий звонок
- **THEN** все три отметки сохраняются вместе со звонком

#### Scenario: Звонок без результата
- **WHEN** менеджер завершает звонок по организации "ООО Ромашка"
- **AND** не выбирает рассылку, сделку и следующий звонок
- **THEN** фиксируется только факт звонка (кто и когда)

### Requirement: Доступ к звонкам ограничен областью доступа
The system SHALL allow a manager to plan, run, and record calls only for
organizations in their access scope (`adr/0007`), and SHALL deny access to
calls of organizations outside it with HTTP 403.

#### Scenario: Менеджер не может запланировать звонок недоступной организации
- **WHEN** в системе существует организация "ООО Конкурент", отсутствующая в области доступа менеджера
- **AND** менеджер пытается запланировать звонок для этой организации
- **THEN** система отклоняет запрос с ошибкой 403
- **AND** звонок не создаётся

#### Scenario: Менеджер не видит недоступные организации в обзвоне
- **WHEN** в системе существует организация "ООО Конкурент", отсутствующая в области доступа менеджера
- **AND** менеджер открывает список обзвона и псевдогруппы
- **THEN** организация "ООО Конкурент" не отображается в списке и псевдогруппах

#### Scenario: Менеджер не может зафиксировать результат звонка недоступной организации
- **WHEN** в системе существует организация "ООО Конкурент", отсутствующая в области доступа менеджера
- **AND** менеджер пытается зафиксировать результат звонка этой организации
- **THEN** система отклоняет запрос с ошибкой 403
- **AND** результат звонка не сохраняется

### Requirement: Завершение обзвона и запуск рассылки
When the manager finishes the session (all organizations called or early
termination), the system SHALL open the launch window with the letters for
organizations marked with a campaign result.

#### Scenario: Запуск рассылки по итогам обзвона
- **WHEN** менеджер завершил обзвон
- **AND** организации "ООО Ромашка" и "ООО А" отмечены рассылкой "Новые курсы"
- **THEN** в окне запуска отображаются письма для организаций "ООО Ромашка" и "ООО А" с возможностью их правки

#### Scenario: Досрочное завершение обзвона
- **WHEN** менеджер прерывает обзвон до прохождения всех организаций
- **AND** хотя бы одна организация уже отмечена рассылкой
- **THEN** открывается окно запуска рассылки для уже отмеченных организаций